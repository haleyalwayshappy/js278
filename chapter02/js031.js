/* 031. 문자열에서 문자 다루기 (문자열에서 문자 추출하기) */
// 문자열.charAt([인덱스; 생략가능])  // 지정한 인덱스의 문자를 추출 // 반환 : 문자열

'JavaScript'.charAt(3); //0번부터 시작해서 찾는다 . 답 :a
'JavaScript'.charAt(); // 인수를 생략할 시 기본값은 인덱스 0에 위치한다. 답 : J

